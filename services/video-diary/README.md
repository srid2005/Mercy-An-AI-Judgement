# video-diary (planned)

Owns: Meera's daily video diary recordings (local copies -- the continuation
lives in `cloud`). Early entries are mundane; later ones cover her father's-
death investigation and her growing fear that Rahul is watching her.

- Evidence prefix: `VID-`
- Own DB: recordings (date, transcript, mood/tag, local_only boolean)
- Player-auth lock: password `NeverForget2018`, pattern leaked via the
  `meera-rahul` DM thread in `social-media` (SOC-021..024) and the year from
  `case-files`.
- Follows the same shape as `services/social-media`.

Not implemented yet.
