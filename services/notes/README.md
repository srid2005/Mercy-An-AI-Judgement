# notes (planned)

Owns: the very first puzzle. Ordinary notes (shopping lists, work, the dog,
family reminders) plus one note that encodes the pattern for the protected
case folder password (`1708RoseCafe` -- dad's birthday + where Arjun and
Meera met).

- Evidence prefix: `NOT-`
- Own DB: notes (title, body, created_at)
- No player-auth lock -- this is the entry point, open by default.
- Follows the same shape as `services/social-media`.

Not implemented yet.
