# cloud -- superseded by `services/haven`

The cloud backup of the video diary is implemented as **Haven**, its in-world
name: see `services/haven/README.md`. Evidence prefix is `HAV-` (not `CLD-`).

This folder is kept only so the planned-services list in `/ARCHITECTURE.md`
still resolves; there is nothing to build here.
