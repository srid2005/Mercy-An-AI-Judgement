The rescue video goes here as `rescue.mp4` (or `.webm`, `.mov`, `.ogv`); `GET /video/rescue` serves whichever exists first, and a generated placeholder plays when none does.
The shot list, format and prompts are in `../../RESCUE_VIDEO.md`.
